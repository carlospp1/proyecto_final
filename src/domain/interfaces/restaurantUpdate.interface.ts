export interface RestaurantUpdate {
    id: string; // El ID del restaurante a actualizar
    name: string; // El nombre del restaurante a actualizar
    address: string; // La dirección del restaurante a actualizar
    cuisine: string; // La especialidad culinaria del restaurante a actualizar
}